{% extends 'base.html.twig' %}

{% block title %}Nouveau mot de passe — CF2m Centre de Formation{% endblock %}

{% block body_class %}page-login{% endblock %}

{% block body %}

<section class="cf2m-login-section">

    <div class="cf2m-login-blob cf2m-login-blob--tl" aria-hidden="true"></div>
    <div class="cf2m-login-blob cf2m-login-blob--br" aria-hidden="true"></div>

    <div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh; padding-top: 70px;">
        <div class="cf2m-login-card">

            <div class="cf2m-login-header">
                <img src="{{ asset('images/logo-cf2m.svg') }}"
                     alt="Logo CF2m"
                     width="108" height="108"
                     class="cf2m-login-logo">
                <h1 class="cf2m-login-title">Nouveau mot de passe</h1>
                <p class="cf2m-login-subtitle">Choisissez un mot de passe sécurisé (8 caractères minimum)</p>
            </div>

            {# Erreurs #}
            {% for error in errors %}
                <div class="cf2m-login-error" role="alert">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
                         fill="none" stroke="currentColor" stroke-width="2.5"
                         stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/>
                        <line x1="12" y1="16" x2="12.01" y2="16"/>
                    </svg>
                    {{ error }}
                </div>
            {% endfor %}

            <form method="post" novalidate data-turbo="false">

                <div class="cf2m-login-field">
                    <label for="new_password" class="cf2m-login-label">Nouveau mot de passe</label>
                    <div class="cf2m-login-input-wrap">
                        <svg class="cf2m-login-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16"
                             viewBox="0 0 24 24" fill="none" stroke="currentColor"
                             stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
                            <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                        </svg>
                        <input type="password"
                               id="new_password"
                               name="new_password"
                               class="cf2m-login-input"
                               placeholder="••••••••"
                               autocomplete="new-password"
                               minlength="8"
                               required>
                    </div>
                </div>

                <div class="cf2m-login-field">
                    <label for="confirm_password" class="cf2m-login-label">Confirmer le mot de passe</label>
                    <div class="cf2m-login-input-wrap">
                        <svg class="cf2m-login-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16"
                             viewBox="0 0 24 24" fill="none" stroke="currentColor"
                             stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
                            <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                        </svg>
                        <input type="password"
                               id="confirm_password"
                               name="confirm_password"
                               class="cf2m-login-input"
                               placeholder="••••••••"
                               autocomplete="new-password"
                               minlength="8"
                               required>
                    </div>
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('reset_password_' ~ token) }}">

                <button type="submit" class="cf2m-login-btn">
                    Enregistrer le nouveau mot de passe
                </button>

            </form>

            <p class="cf2m-login-back">
                <a href="{{ path('app_login') }}">← Retour à la connexion</a>
            </p>

        </div>
    </div>

</section>

{% endblock %}
